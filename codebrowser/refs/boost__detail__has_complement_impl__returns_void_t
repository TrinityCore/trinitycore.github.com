<def f='include/boost/type_traits/detail/has_prefix_operator.hpp' l='76'/>
<size>1</size>
<doc f='include/boost/type_traits/detail/has_prefix_operator.hpp' l='67'>// 3. checks if the operator returns void or not
// conditions: Rhs!=void

// we first redefine &quot;operator,&quot; so that we have no compilation error if
// operator BOOST_TT_TRAIT_OP returns void and we can use the return type of
// (BOOST_TT_TRAIT_OP rhs, returns_void_t()) to deduce if
// operator BOOST_TT_TRAIT_OP returns void or not:
// - operator BOOST_TT_TRAIT_OP returns void   -&gt; (BOOST_TT_TRAIT_OP rhs, returns_void_t()) returns returns_void_t
// - operator BOOST_TT_TRAIT_OP returns !=void -&gt; (BOOST_TT_TRAIT_OP rhs, returns_void_t()) returns int</doc>
