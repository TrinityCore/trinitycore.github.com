<def f='include/boost/concept_archetype.hpp' l='263'/>
<size>1</size>
<doc f='include/boost/concept_archetype.hpp' l='250'>// The purpose of the optags is so that one can specify
  // exactly which types the operator&lt; is defined between.
  // This is useful for allowing the operations:
  //
  // A a; B b;
  // a &lt; b
  // b &lt; a
  //
  // without also allowing the combinations:
  //
  // a &lt; a
  // b &lt; b
  //</doc>
